package com.company;

// nome (string), categoria(enum), Tempo de preparo(double), rendimento(classe),
// Ingrediente(classe), Preparo(classe)

// Rendimento: quantidade(int),tipo(enum);

//Ingrediente: nome(string), quantidade(double), tipo(enum);

public class Main {

    public static void main(String[] args) {
	// write your code here
    }
}
